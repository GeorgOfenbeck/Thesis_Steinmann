<?xml version="1.0" encoding="UTF-8"?>
<derivedClass
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:noNamespaceSchemaLocation="../shared.xsd"
	name="RunCommandConfigurator" 
	baseType="ConfiguratorBase"
	comment="configurator configuring the system">
	
	<list name="beforeMeasurementCommands" type="RunCommand" comment="commands to run before the measurement starts"/>
	<list name="afterMeasurementCommands" type="RunCommand" comment="commands to run after the measurement completed"/>
	<list name="beforeRunCommands" type="RunCommand" comment="commands to run before each measurement run"/>
	<list name="afterRunCommands" type="RunCommand" comment="commands to run after each measurement run"/>
	
</derivedClass>