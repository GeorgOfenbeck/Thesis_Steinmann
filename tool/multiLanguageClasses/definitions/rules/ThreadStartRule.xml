<?xml version="1.0" encoding="UTF-8"?>
<derivedClass
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:noNamespaceSchemaLocation="../shared.xsd"
	name="ThreadStartRule" 
	baseType="RuleBase"
	comment="Rule matching a thread start">

	<field name="parentTid" type="int" comment="match only if the clone was invoked by the thread with this tid. -1 matches any thread"/>
	<field name="workloadId" type="int" comment="match only if the thread was caused to be started by the workload with this id. -1 matches any workload"/>	
</derivedClass>